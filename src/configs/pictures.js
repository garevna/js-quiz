import { getHost } from '@/globalState'

export const pictures = {
  navigationPanelIcon: `${getHost()}/images/js-icon.svg`,
  personage: `${getHost()}/images/personage.gif`,
  garevna: `${getHost()}/images/garevna.png`,
  scoreIcon: `${getHost()}/icons/trophy.png`,
  success: `${getHost()}/images/success.jpg`,
  failure: `${getHost()}/images/failure.gif`,
  final: `${getHost()}/images/matrix.webp`
}
