§quizType
findError

§quizBalls
4

§quizQuestion
Допилите код, чтобы возвращался минимальный элемент массива


§quizJS
[9, 4, 7, 5, 8]
  .reduce((prev, curr) => •••)

§rightContent
[9, 4, 7, 5, 8]
  .reduce((prev, curr) => Math.min(prev, curr))
